// Replace CHAT_ENDPOINT with the URL of your deployed Cloudflare Worker
// (e.g. https://allin-chat.<your-subdomain>.workers.dev).
// Leave it as "" to disable the chatbot while you set things up.
window.APP_CONFIG = {
  CHAT_ENDPOINT: ""
};
